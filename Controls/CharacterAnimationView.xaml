<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="TaikoDiveLauncher.Controls.CharacterAnimationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid MinHeight="220">
        <Viewbox
            MaxWidth="360"
            MaxHeight="280"
            HorizontalAlignment="Center"
            Stretch="Uniform">
            <Grid
                Width="320"
                Height="252">
                <Grid.Clip>
                    <RectangleGeometry Rect="0,0,320,252" />
                </Grid.Clip>
                <Image
                    x:Name="PreviewImage"
                    Width="640"
                    Height="360"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Stretch="Uniform" />
            </Grid>
        </Viewbox>
        <TextBlock
            x:Name="EmptyMessage"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="normal アニメが見つかりません"
            TextAlignment="Center"
            TextWrapping="Wrap"
            Visibility="Collapsed" />
    </Grid>
</UserControl>
